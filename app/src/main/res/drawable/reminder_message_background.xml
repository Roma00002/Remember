<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/reminder_background" />

    <!-- Corner radius -->
    <corners android:radius="@dimen/message_corner_radius" />

    <!-- Left border accent for reminders -->
    <stroke
        android:width="3dp"
        android:color="@color/reminder_border" />

</shape>